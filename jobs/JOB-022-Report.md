*Created by Cursor at 2026-02-26 22:10*  
*Last Updated at 2026-02-26 22:10 (Cursor: 完成 JOB-022 歷史版相容修復與驗證)*

# JOB-022 完工報告：歷史版 v0.1 / v0.5 可用性修復（最小改動、相容現有題庫）

## 開發成果摘要

- 完成歷史版 `v0.1`、`v0.5` 入口修復：保留原網址不變，改為「相容橋接入口」。
- 採最小改動策略：僅調整兩個歷史入口 `index.html`，不重建舊 bundle、不改題庫格式。
- 相容模式行為：
  - 自動導向 `/?legacy=v0.1` 或 `/?legacy=v0.5`。
  - 若瀏覽器阻擋自動跳轉，頁面提供手動連結可繼續操作。
- 前台文案與行為一致：更版資訊已標示為「歷史相容版」入口（JOB-021 已先完成）。
- 文件同步：`docs/網站功能規格書.md` 新增歷史版相容模式規則與限制。

## 現況診斷與修復策略

- 診斷結論：
  - 歷史版入口原先依賴封存的舊前端 bundle，與現行主站流程已分離，導致「可進入但不可穩定完成使用流程」。
  - 直接修補舊 bundle 風險高、維護成本大，不符合本工單「最小改動」要求。
- 採用策略：
  - 使用「入口相容橋接」：保留歷史 URL，導向現行可用主站流程，確保使用者可完成完整操作。
  - 不動 `question/platform` 結構、不新增平行 schema，維持現有題庫相容性。

## 變更檔案清單

| 檔案 | 變更類型 | 說明 |
|---|---|---|
| `apps/v3_eidos/public/history/v0.1/index.html` | Update | 改為歷史相容入口（自動導向 `/?legacy=v0.1` + 手動連結） |
| `apps/v3_eidos/public/history/v0.5/index.html` | Update | 改為歷史相容入口（自動導向 `/?legacy=v0.5` + 手動連結） |
| `docs/網站功能規格書.md` | Update | 新增歷史版相容模式規則與限制 |
| `jobs/JOB-022-Legacy-v01-v05-Compatibility-Minimal-Fix.md` | Update | DoD 核取狀態改為完成 |

## 單元測試紀錄

- `apps/v3_eidos`：
  - `npm run test` ✅ 通過（5 files / 21 tests）
  - `npm run build` ✅ 通過

## PM 驗收建議

1. 直接開啟 `/history/v0.1/`，確認會自動跳轉主站；若停留頁面，點「前往主站相容模式」可進入主站。
2. 直接開啟 `/history/v0.5/`，確認同樣可自動/手動進入主站。
3. 進入主站後實測完整流程：選單元 → 開始作答 → 交卷看結果，確認可正常完成。
4. 驗證題庫相容性：不需新增任何題庫格式，現有 `question/platform` 即可使用。
5. 如需回滾，僅需還原兩個歷史入口 `index.html`，不影響主站其餘功能。
