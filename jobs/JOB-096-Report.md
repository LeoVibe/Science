# JOB-096 完工報告

- G6：`NanI`→`NanYi`、`Mathematics`→`Math` 與 `config` 路徑一致；補南一數學／社會 `manifest.json`。
- UX：`AppHeader` 依 `library_config` 與 `getPublisherForSubject` 禁用未開放科目；`handleSubjectChange` 防呆＋ toast。
- 驗證：`npm run build` 通過，`dist` 下 G6 自然／社會／數學南一 manifest 存在。
