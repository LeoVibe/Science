# JOB-047 完工報告：出題原則極簡化 UI 優化

*Completed by Antigravity at 2026-02-28 21:57*

## 開發成果摘要

### 1. 視覺噪音消除
- 移除原本位於 `MainMenu.tsx` 標題下的寬版 `bg-muted/30` 簡介區塊。
- 改為將入口按鈕「💡 出題原則」縮小並嵌入至課程資訊列（Sub-header）右側。

### 2. 精緻化 UI 實作
- **版面布局**：使用 `flex-wrap` 與 `items-center` 確保在小螢幕上資訊列與按鈕能自動折行。
- **色彩計畫**：選用 `bg-amber-50` 與 `text-amber-600`，營造出如同「重點提示」或「高品質保證」的溫暖質感，與課程藍色調形成互補。
- **互動反饋**：點擊感優化（`hover:bg-amber-100`），並維持與既存 `SubjectPrincipleDrawer` 的聯動。

## 變更檔案清單
| 檔案 | 變更類型 | 說明 |
|------|:---:|------|
| `apps/v3_eidos/src/components/MainMenu.tsx` | 修改 | UI 重構：區塊轉化為行內按鈕 |

## 驗收建議
1. 進入任何科目頁面。
2. 確認標題與綜合練習按鈕之間，原本的大方塊已消失。
3. 確認在年級版本資訊（如：三年級 國語...）右側出現了一個精巧的 `💡 出題原則` 按鈕。
4. 點擊測試 Drawer 動效。

**結案狀態：🟢 DONE**
