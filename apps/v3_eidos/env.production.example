# 正式機建置用環境變數（複製為 .env.production 或由 CI 注入）
# 本專案 GitHub Actions 已在 deploy.yml 設定，無需手動建檔。
VITE_APP_BASE=/Science/
VITE_API_URL=https://eidos-api.eidosedu.workers.dev

# 後台 Google OAuth（與 Google Cloud Console Web Client ID 一致；Cloudflare Pages / GitHub Secrets 必設）
# VITE_GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
