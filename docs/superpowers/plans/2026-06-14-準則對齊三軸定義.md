# 準則對齊三軸定義 Implementation Plan

> **For agentic workers:** 逐 task 執行。本計劃修改**準則文件**，須在 docs_ops JOB 下進行（CLAUDE.md §3.1.3）。每段改動標「目標段／改後內容」；執行時先 Read 該段取得精確 old_string 再 Edit。

**Goal:** 把三軸定義（成熟度 RM／品質 QL／上架 is_publishable）落到所有準則文件，消除「QL3 不能上架」「統計灌水」「RM=QL」的矛盾。

**依據 spec:** `docs/superpowers/specs/2026-06-14-品質與上架層次定義-design.md`

**原則:** 定義只寫在驗證盲測準則 §4（唯一真相）；其他文件改成「指過去看」+ 對齊措辭。對外 QL1–QL5 文字一律不動。

---

### Task 0: 開 docs_ops JOB
- [ ] `node scripts/job_manager.js next` → create「準則對齊三軸定義」DEV，job_type 註明 `docs_ops`
- [ ] 填派工單（目標/邊界：只改準則措辭與上架定義，不動 QL1-5 對外文字、不改題庫資料）

---

### Task 1: 驗證盲測準則（QL/上架的唯一真相）— 主改
**檔案:** `question/README_驗證與盲測準則.md`

- [ ] **§4.6 上架門檻**　目標段：現「單題上架 is_publishable===true…／單課≥25／題庫 QL 升級」三行。
  改後內容：
  ```
  ## 上架兩級制
  - QL4 庫 → 正式上架（前端無標記）
  - QL3 庫 → BETA 上架（前端標「BETA · 尚未嚴謹測試」）
  - QL2 以下 → 不上架
  單題上架：is_publishable === true（見 §2.5）
  單課上線：is_publishable===true 題數 ≥ 25
  對外上架數：一律＝ is_publishable===true 實際題數，禁止以「整包達標即全算」估計
  ```
- [ ] **§2.5 單題 is_publishable 判定**　目標：現有判定表。
  補兩列：QL3 達標（有考古題研究）且 review_status 非 pending → is_publishable 可設 true（標 BETA）；任何題即使 QL4，發現特殊問題可單題設 false 獨立下架（不影響同課其他題）。
- [ ] **§4.5**　補一句：is_publishable 是獨立上架閘門，不是 QL 的自動結果。
- [ ] 驗證：`grep -n "兩級\|BETA\|獨立下架" question/README_驗證與盲測準則.md` 有命中。

---

### Task 2: 研究架構總綱（RM=素材潛力）— 主改
**檔案:** `knowledge/README_研究架構總綱.md`（RM 表 line 120-129）

- [ ] 表頭「對應品質」欄改為「品質天花板（潛力上限）」。
- [ ] 表格下方加註：
  ```
  > RM 是「素材成熟度（潛力）」，記錄研究素材備到多齊。素材越好越**有機會**做出高 QL 題，
  > 但**不保證**——最終 QL 仍需出題與盲測實際達成。好素材 ≠ 好題庫。
  > RM 與 QL 是兩條獨立的軸：RM 看投入（素材），QL 看產出（成品）。
  ```
- [ ] 驗證：grep「潛力上限\|好素材」命中。

---

### Task 3: 出題品管準則（CQI 定位）
**檔案:** `question/README_出題與品管準則.md`（第一章 CQI-P 開頭）

- [ ] 章首補一句定位：
  ```
  > CQI 是**內部流程分數與關卡**（CQI-P ≥ 5.5 才進盲測），不對外顯示、不直接定義 QL。
  > 對外品質以 QL 為準（見驗證盲測準則 §4），上架以 is_publishable 為準（見 §4.6）。
  ```
- [ ] 驗證：grep「內部流程分數」命中。

---

### Task 4: README.md（§四流水線、§五品質定義）
**檔案:** `README.md`

- [ ] §四品管流水線「QL 上架」那列：由「CQI ≥ 6.5 = QL4 可上架」改為「QL4 正式上架／QL3 BETA 上架（見驗證盲測準則 §4.6）」。
- [ ] **§五 QL1-5 表文字一律不動**（對外維持）。
- [ ] 驗證：§五 QL1-5 文字 git diff 無變動；§四出現「BETA」。

---

### Task 5: CLAUDE.md（§四題庫完成度判定）
**檔案:** `CLAUDE.md`

- [ ] §四四條件下補一句：
  ```
  > 上架以 is_publishable 為準：盲測過 ≠ 一定上架（可能 review pending 或因特殊問題被單題下架）。
  > QL3 可 BETA 上架、QL4 正式上架（見 question/README_驗證與盲測準則.md §4.6）。
  ```
- [ ] 驗證：grep「BETA 上架\|is_publishable 為準」命中。

---

### Task 6: 網站功能規格書（§2.2.1 BETA）
**檔案:** `docs/網站功能規格書.md`

- [ ] §2.2.1 BETA 段補一句對齊官方說法：「QL4＝正式（無標記）、QL3＝BETA（標尚未嚴謹測試）、QL2 以下不上架。」
- [ ] 驗證：grep「QL4＝正式\|QL3＝BETA」命中。

---

### Task 7: 產品介紹（對外，QL 維持）
**檔案:** `docs/README_產品介紹.md`（第四層 line 66+、QL 表 line 79+）

- [ ] 第四層「上架」說明補白話一句：「通過盲測的題正式上線；研究紮實但尚未盲測的題以 BETA 形式提供，並明確標示。」
- [ ] **QL 表（line 79+）文字不動**。
- [ ] 驗證：QL 表 git diff 無變動。

---

### Task 8: 結案
- [ ] 各檔改完，逐項 grep 驗證命中。
- [ ] 草擬 commit 訊息（docs type）給使用者確認 → commit。
- [ ] JOB 結案五步（close→pj_sync→commit→Discord）。

---

## 範圍外（不在本計劃，另開單）
- 程式落實「QL3 題回寫 is_publishable=true 標 BETA」（讓 QL3 實際上線）— 需 question_verify/engineering JOB。
- generate_library_stats 統計對齊已於 JOB-258 完成。

## Self-Review
- spec 三軸 → Task 1(上架)、Task 2(RM)、Task 3(CQI/QL)；唯一真相原則 → 定義集中 §4，其他指連結。
- QL1-5 對外不動 → Task 4/7 明確標「文字不動」+ git diff 驗證。
- 無 placeholder（新內容均具體）；old_string 執行時 Read 取得（已標目標段）。
