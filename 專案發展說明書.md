# eidos 專案發展說明書 (Project Specification)

**專案背景：** 
本站的起始，只是2026過年的期間的 vibe coding 練習，
原始動機就是發現兒子常常考試很粗心，希望能有多一點的練習時間。
純粹是設計題目給自己小三的兒子刷題玩，無任何營利想法與目標

本站從發想到實作皆由 Antigravity 與 Cursor 協作開發，
後續加入 Lovable 與 Gemini 進行科目研究與題目設計

台灣目前有 許多優質教育網站，但本站著重在題庫練習之用，未來會強化題目意涵
目前類似的網站有 高中國中國小題庫(www.tcool.cc) ＆ 學習方舟(www.studyark.org)
但本站只希望 操作可以更簡單，省心一點，每天用回家5分鐘，刷個題


後續有機會，在持續打造一個具備高度擴充性、AI 驅動的國小課後複習平台。
透過「服務層」與「題庫層」分離，達成教材研究深度化與複習作業的彈性發展。
最終期待透過老師與家長對課程的理解，共創更有深度與意義的學習題庫


---

## 1. 系統架構概要 (Technical Architecture)
* **基礎設施：** Cloudflare 生態系 (Pages, Workers, D1 Database, KV, R2 Storage)。
* **開發模式：** Monorepo (管理 v1, v2, eidos 三個前端應用)。
* **核心邏輯：**
    * **Knowledge Base (Markdown):** 儲存教材研究文稿（由 Gemini/Antigravity 維護）。
    * **Assets (JSON):** 由研究文稿編譯而成的標準化題庫資產，採靜態讀取以優化效能。
    * **Adapter Pattern:** 確保新舊版本網站皆能讀取共用題庫，實現「向下相容」。

---

## 2. AI 團隊角色分工 (AI Team Roles)

| 角色 | 職位 | 核心任務 |
| :--- | :--- | :--- |
| **Antigravity** | **大 PM & 總校閱** | 定義功能需求、設計題庫規範 (Schema)、審核題目邏輯、管理校驗規則、執行最終白箱測試。 |
| **Gemini** | **教育研究員** | 深入分析課文、制定出題思路、產出高密度原始題目（Markdown/YAML 格式）。 |
| **Cursor** | **首席開發者** | 建立 Monorepo、實作 Cloudflare API、撰寫轉換腳本 (Parser)、開發後台管理功能。 |
| **Lovable** | **UI/UX 設計師** | eidos 視覺設計、多版本入口介面、後台管理與「題庫檢查儀表板」互動設計。 |

---

## 3. 核心流程：自動化生產管道 (The Pipeline)

### 第一階段：規格與研究 (Definition)
1. **Antigravity** 定義 `eidos v2.0` 題庫規格書與功能白皮書。
2. **Cursor** 根據規格建立 `schema.json` 與 D1 資料庫結構。
3. **Gemini** 進行課文深度研究，產出結構化文稿。

### 第二階段：生產與編譯 (Production)
1. **Gemini** 根據新思路與文稿產出原始題項內容。
2. **Cursor** 透過轉換腳本將內容封裝成「靜態 Assets (JSON)」。
3. **自動化驗證：** 腳本自動檢查格式是否符合 Schema，若錯誤則退回。

### 第三階段：開發與部署 (Development)
1. **Lovable** 交付 eidos 前端與後台 UI。
2. **Cursor** 整合代碼，並確保 v1, v2 的 Adapter 能讀取新資產。

### 第四階段：手動校驗與 QA (QA Dashboard)
1. **功能：** 管理員可在後台「一鍵觸發」題庫健康檢查。
2. **邏輯：**
    * **靜態檢查：** 檢查圖片路徑、Schema 完整性（由程式執行）。
    * **邏輯檢查：** 由 Gemini 針對題目唯一解、教學語境進行掃描。
3. **驗收：** **Antigravity** 進行最終功能完整性測試。

---

## 4. 各角色具體行動指令 (Action Items)

### **To: Gemini**
- 產出高密度核心內容，專注於教育邏輯，不需處理複雜 JSON 結構。
- 在研究文稿中需包含「教學目標」與「版本標籤」。

### **To: Antigravity**
- 立即起草「eidos 題庫規格書」。
- 列出「後台校驗邏輯」的檢查清單（如：重複率、難度標註正確性）。

### **To: Cursor**
- 初始化 Monorepo 架構。
- 開發「Markdown 研究文稿 -> JSON Assets」的轉換程式。
- 實作 Cloudflare Worker API 以支援後台手動檢查功能。

### **To: Lovable**
- 設計 eidos 主視覺。
- 設計「題庫檢查儀表板」，讓複雜的校驗結果一眼即知。

---

## 5.測試驅動與品質保證 (TDD & QA)

#### 測試開發職責
* **Antigravity (PM)：** 負責撰寫「測試情境說明書」。定義功能運作的正確邊界，作為 Cursor 開發測試代碼的唯一依據。
* **Cursor (Dev)：** 實作自動化測試框架。確保每次代碼更新或題庫轉換後，皆能自動執行單元測試。
* **Gemini (Content)：** 生成測試用偽數據 (Mock Data)，模擬家長輸入各種非標準化想法時，系統的反應能力。

#### 後台測試運作機制
1. **一鍵自檢 (One-Click Health Check)：** 管理後台整合測試運行器，可隨時針對「題庫層」與「服務層」進行斷言測試 (Assertion)。
2. **多版本回歸測試：** 每次題庫更新，系統自動模擬舊版網頁的請求，確保「向下相容性」未受損。



